{

  "scripts": {
  "prebuild": "apt-get update && apt-get install -y python3 python3-pip build-essential libcairo2-dev libpango1.0-dev libjpeg-dev libgif-dev librsvg2-dev",
  "build": "npm install"
},
  "dependencies": {
    "@google/generative-ai": "^0.24.0",
    "canvas": "^3.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "http": "^0.0.1-security",
    "mime-types": "^3.0.1",
    "ws": "^8.18.1",
    "y-websocket": "^1.3.12"
  }
}
